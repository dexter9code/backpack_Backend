header.header 
    nav.header__logo
        //- a(href='#').nav__el Adding Tours
        img(src='icons/backpack-logo.png' alt='logo')
    .header__logo 
    nav.nav.nav--user 
        if user 
            a(href='/me').nav__el
                img.nav__user-img(src=`${user.photo}` alt=`/img/users/default.jpg`)
                span=user.name.split(' ')[0]
            a#logoutBTN.nav__el.nav__el--logout Logout

        else 
            a(href='/login').nav__el.nav__el--cta Login

